apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  hostnames:
    - argo.kroknes.dev
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: eg
      namespace: default
      sectionName: http
  rules:
    - backendRefs:
        - kind: Service
          namespace: argocd
          name: argocd-server
          port: 80
      matches:
        - path:
            type: PathPrefix
            value: /
